name: release

# Single end-to-end release pipeline.
#
# Triggered when release-please pushes a `vX.Y.Z` tag (or via manual
# dispatch with the same tag). Runs three stages:
#
#   1. build       — matrix-build all 5 platform tarballs + napi .nodes
#                    + maturin wheels on native runners. ort's prebuilt
#                    ONNX Runtime links cleanly only on native, so we
#                    don't cross-compile anything here.
#   2. commit      — collect every artifact into `release_artifacts/<tag>/` on main
#                    and push a `chore(release): vX.Y.Z artifacts [skip ci]`
#                    commit. Git LFS handles the actual binary storage
#                    so main's git history stays small.
#   3. release     — create the GitHub Release with the same artifacts
#                    attached. Publishing the release fans out to
#                    `release-rust.yml`, `release-pypi.yml`,
#                    `release-npm.yml` which push to crates.io / PyPI /
#                    npm / Homebrew.

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v0.7.0)"
        required: true

permissions:
  contents: write

jobs:
  build:
    name: build ${{ matrix.friendly }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-14,         target: aarch64-apple-darwin,        friendly: macos-arm64,    rustflags: "-C target-cpu=apple-m1" }
          - { os: ubuntu-latest,    target: x86_64-unknown-linux-gnu,    friendly: linux-x64,      rustflags: "-C target-cpu=haswell" }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-gnu,   friendly: linux-arm64,    rustflags: "-C target-cpu=cortex-a72" }
          - { os: windows-latest,   target: x86_64-pc-windows-msvc,      friendly: windows-x64,    rustflags: "-C target-cpu=haswell" }
          - { os: windows-11-arm,   target: aarch64-pc-windows-msvc,     friendly: windows-arm64,  rustflags: "" }
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
      - name: build cli + claude-code
        env:
          RUSTFLAGS: ${{ matrix.rustflags }}
        shell: bash
        run: cargo build --release --target ${{ matrix.target }} -p lethe-cli -p lethe-claude-code
      - name: package tarball / zip
        shell: bash
        run: |
          set -euo pipefail
          STAGE="lethe-${{ matrix.friendly }}"
          mkdir -p "$STAGE"
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp "target/${{ matrix.target }}/release/lethe.exe"             "$STAGE/"
            cp "target/${{ matrix.target }}/release/lethe-claude-code.exe" "$STAGE/"
          else
            cp "target/${{ matrix.target }}/release/lethe"             "$STAGE/"
            cp "target/${{ matrix.target }}/release/lethe-claude-code" "$STAGE/"
          fi
          cp README.md LICENSE "$STAGE/" 2>/dev/null || true
          if [ "${{ runner.os }}" = "Windows" ]; then
            7z a "${STAGE}.zip" "$STAGE"
          else
            tar -czf "${STAGE}.tar.gz" "$STAGE"
          fi
      - name: build napi .node
        env:
          RUSTFLAGS: ${{ matrix.rustflags }}
        shell: bash
        run: |
          set -euo pipefail
          cargo build --release --target ${{ matrix.target }} -p lethe-node
          case "${{ runner.os }}" in
            Windows) src="target/${{ matrix.target }}/release/lethe_node.dll" ;;
            Linux)   src="target/${{ matrix.target }}/release/liblethe_node.so" ;;
            *)       src="target/${{ matrix.target }}/release/liblethe_node.dylib" ;;
          esac
          cp "$src" "lethe-${{ matrix.friendly }}.node"
      - name: build maturin wheel
        uses: PyO3/maturin-action@v1
        with:
          working-directory: crates/lethe-py
          target: ${{ matrix.target }}
          args: --release --out wheels --strip
          manylinux: auto
      - name: collect artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          mv lethe-${{ matrix.friendly }}.* out/ 2>/dev/null || true
          mv crates/lethe-py/wheels/*.whl out/ 2>/dev/null || true
          ls -la out
      - uses: actions/upload-artifact@v4
        with:
          name: lethe-${{ matrix.friendly }}
          path: out/

  commit:
    name: commit artifacts to repo
    needs: build
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    steps:
      - id: tag
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          path: release_artifacts/${{ steps.tag.outputs.tag }}
          merge-multiple: true
      - name: commit and push
        run: |
          set -euo pipefail
          git config user.name  "lethe-release-bot"
          git config user.email "lethe-release-bot@users.noreply.github.com"
          git add release_artifacts/
          if git diff --cached --quiet; then
            echo "no artifact changes to commit"
            exit 0
          fi
          git commit -m "chore(release): ${{ steps.tag.outputs.tag }} artifacts [skip ci]"
          git push origin main

  release:
    name: create GitHub Release
    needs: commit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: upload assets and publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.commit.outputs.tag }}
        run: |
          set -euo pipefail
          # release-please created the release as a draft (see
          # `"draft": true` in release-please-config.json). Upload the
          # built artifacts, then flip the draft flag — only at that
          # moment does GitHub fire `release: published`, which is the
          # trigger the registry-push workflows wait for. This avoids
          # the race where they'd otherwise run before any assets exist.
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" --draft --generate-notes
          fi
          gh release upload "$TAG" dist/* --clobber
          gh release edit   "$TAG" --draft=false
